import { SectionHeader } from "src/shared";

import classes from "./experience.module.css";

export const Experience = () => {
  return (
    <div className={classes.experience}>
      <SectionHeader sectionTitle="Experience timeline" />
    </div>
  );
};
